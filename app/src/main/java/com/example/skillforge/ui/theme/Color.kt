package com.example.skillforge.ui.theme

import androidx.compose.ui.graphics.Color

// Drumming-inspired palette

// Light theme colors
val Crimson = Color(0xFFB71C1C)         // Deep red - primary
val CrimsonLight = Color(0xFFD32F2F)    // Lighter red for containers
val Amber = Color(0xFFFF8F00)           // Gold/amber - secondary (cymbals)
val AmberLight = Color(0xFFFFB300)      // Lighter amber
val Charcoal = Color(0xFF37474F)        // Dark grey - tertiary (drum shells)
val CharcoalLight = Color(0xFF546E7A)   // Lighter charcoal

// Dark theme colors (inverted intensity)
val CrimsonDark = Color(0xFFEF5350)     // Brighter red for dark backgrounds
val AmberDark = Color(0xFFFFCA28)       // Brighter gold
val CharcoalDark = Color(0xFF90A4AE)    // Lighter grey for dark mode

// Surfaces
val SurfaceLight = Color(0xFFFFFBF5)    // Warm white (like drum head)
val SurfaceDark = Color(0xFF1C1B1F)     // Near black
